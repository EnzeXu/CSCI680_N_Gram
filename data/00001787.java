public class Lfs extends Hfs { @ ConstructorProperties ( { "scheme" } ) Lfs ( Scheme scheme ) { super ( scheme ) ; } @ ConstructorProperties ( { "scheme" , "stringPath" } ) public Lfs ( Scheme scheme , String stringPath ) { super ( scheme , stringPath ) ; } @ ConstructorProperties ( { "scheme" , "stringPath" , "sinkMode" } ) public Lfs ( Scheme scheme , String stringPath , SinkMode sinkMode ) { super ( scheme , stringPath , sinkMode ) ; } protected void setStringPath ( String stringPath ) { if ( stringPath . matches ( " . * : throw new IllegalArgumentException ( "uri must use the file scheme" ) ; super . setStringPath ( stringPath ) ; } @ Override protected FileSystem getFileSystem ( Configuration conf ) { try { return FileSystem . getLocal ( conf ) ; } catch ( IOException exception ) { throw new TapException ( "unable to get handle to get local filesystem" , exception ) ; } } }